<?php
// إعدادات التجزئة / Hashing Configuration

return [

    /*
    |----------------------------------------------------------------------
    | سائق التجزئة الافتراضي / Default Hash Driver
    |----------------------------------------------------------------------
    |
    | خوارزمية التجزئة المستخدمة لتشفير كلمات المرور
    | Hashing algorithm used for password encryption
    |
    */

    'driver' => env('HASH_DRIVER', 'bcrypt'),

    /*
    |----------------------------------------------------------------------
    | إعدادات bcrypt / Bcrypt Options
    |----------------------------------------------------------------------
    */

    'bcrypt' => [
        'rounds' => env('BCRYPT_ROUNDS', 12),
        'verify' => env('HASH_VERIFY', true),
    ],

    /*
    |----------------------------------------------------------------------
    | إعدادات Argon2 / Argon2 Options
    |----------------------------------------------------------------------
    */

    'argon' => [
        'memory' => env('ARGON_MEMORY', 65536),
        'threads' => env('ARGON_THREADS', 1),
        'time' => env('ARGON_TIME', 4),
        'verify' => env('HASH_VERIFY', true),
    ],

    /*
    |----------------------------------------------------------------------
    | إعدادات تجزئة بدون تشفير / Plain Text Options
    |----------------------------------------------------------------------
    */

    'plaintext' => [
        'verify' => false,
    ],

];
